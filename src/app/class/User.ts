export class User {
    nom: string;
    prenom: string;
    contact: string;

    constructor(nom: string, prenom: string, contact: string) {
        this.nom = nom;
        this.prenom = prenom;
        this.contact = contact;
    }
}